---
"tracing": minor
"tracing-js": minor
---

Add `with_layer()` method and `BoxedLayer` type alias for custom tracing layer support. Users can now add their own `tracing_subscriber::Layer` implementations to the subscriber stack.
